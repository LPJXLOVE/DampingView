<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <declare-styleable name="DampingView">
        <attr name="Text" format="string"></attr>
        <attr name="TextColor" format="color"></attr>
        <attr name="TextSize" format="dimension"></attr>
    </declare-styleable>

    <declare-styleable name="LoadLayout">
        <attr name="LoadText" format="string"></attr>
        <attr name="LoadTextColor" format="color"></attr>
        <attr name="LoadTextSize" format="dimension"></attr>
        <attr name="LoadImage" format="reference"></attr>
    </declare-styleable>

</resources>
